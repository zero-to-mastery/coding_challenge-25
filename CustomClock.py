import turtle
import time

# Start off with a simple clock
window = turtle.Screen()
window.bgcolor("white")
window.setup(width=500, height=500)
window.title("Custom Clock Made By @Kooki-eByte")

# Work on a more dynamic clock to have different designs or colors for user to customize 


window.mainloop() # >>> Allows for the window to stay on after it finishes its initial boot up.